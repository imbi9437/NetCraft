# 🌐 **네트워크 게임 플로우차트**

## 📋 **전체 게임 플로우**

```
┌─────────────────────────────────────────────────────────────────┐
│                        🎮 게임 시작 플로우                        │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   📱 Title 신    │───▶│   🏠 Main 신     │───▶│  🎯 Game 신     │
│                 │    │                 │    │                 │
│ • 로그인        │    │ • 방 생성/참가   │    │ • 플레이어 스폰  │
│ • 네트워크 연결  │    │ • Ready 대기     │    │ • 게임 플레이    │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

---

## 🔄 **신 전환 시 초기화 플로우**

```
PhotonNetwork.LoadLevel("GameScene")
                    ↓
┌─────────────────────────────────────────────────────────────────┐
│                    🚀 새 씬 로드 완료                            │
└─────────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────────┐
│              📋 DefaultExecutionOrder 초기화 순서                │
└─────────────────────────────────────────────────────────────────┘

[-100] EventHub.Awake()
        ↓
[-99]  SceneController.Awake()
        ↓
[-90]  NetworkManager.Awake() + DataManager.Awake()
        ↓
[-80]  MainMenuPanel.Awake() + NetworkItemManager.Awake()
        ↓
[-60]  NetworkWorldManager.Awake()
        ↓
[-50]  InputManager.Awake() + GameManager.Awake() + NetworkPlayerInteraction.Awake()
        ↓
[-45]  NetworkPvESystem.Awake()
        ↓
[-40]  NetworkGameEventManager.Awake()
        ↓
플레이어 스폰 시:
[-100] PlayerStateMachine.Awake()
        ↓
[100]  PlayerCameraController.Awake()
```

---

## 👤 **플레이어 스폰 상세 플로우**

```
MultiplayerGameController.StartGame()
                    ↓
PlayerSpawner.SpawnLocalPlayer()
                    ↓
PhotonNetwork.Instantiate("Player", spawnPosition, Quaternion.identity)
                    ↓
┌─────────────────────────────────────────────────────────────────┐
│                    🎭 플레이어 컴포넌트 초기화                    │
└─────────────────────────────────────────────────────────────────┘
                    ↓
PlayerStateMachine.Awake()
├── InitializeComponents()
│   ├── characterController = GetComponent<CharacterController>()
│   ├── animator = GetComponent<Animator>()
│   ├── photonView = GetComponent<PhotonView>()
│   ├── inputHandler = GetComponent<PlayerInputHandler>()
│   ├── networkHandler = GetComponent<PlayerNetworkHandler>()
│   └── cameraController = GetComponent<PlayerCameraController>()
├── SetupInputEvents()
├── InitializePlayerData()
└── SetupGameEventSubscriptions()
                    ↓
PlayerNetworkHandler.Start()
├── SubscribeToGameEvents()
├── SetPlayerBasicInfo() [즉시 실행]
├── Invoke(InitializePlayerStats, 0.2f) [0.2초 후]
└── Invoke(SendInitialStatusToUI, 0.5f) [0.5초 후]
```

---

## 🎮 **플레이어 입력 → 이동 플로우**

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│ 🎯 입력 처리     │───▶│ 🏃 이동 처리     │───▶│ 📡 네트워크 동기화│
│                 │    │                 │    │                 │
│ PlayerInput     │    │ PlayerState     │    │ PlayerNetwork   │
│ Handler         │    │ Machine         │    │ Handler         │
└─────────────────┘    └─────────────────┘    └─────────────────┘
        │                       │                       │
        ▼                       ▼                       ▼
HandleMovementInput()   HandleMoveInput()    OnPhotonSerializeView()
        │                       │                       │
        ▼                       ▼                       ▼
OnMoveInput 이벤트      UpdateMovement()      실시간 데이터 전송
        │                       │                       │
        ▼                       ▼                       ▼
PlayerStateMachine      MovePlayer()          원격 플레이어들
```

---

## 📊 **스탯 변경 플로우**

```
┌─────────────────────────────────────────────────────────────────┐
│                    🎯 스탯 변경 트리거                            │
│  • 환경 효과 (정신력 감소)                                        │
│  • 배고픔/수분 자동 감소                                         │
│  • 플레이어 액션 (음식 섭취 등)                                  │
└─────────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────────┐
│                    🎮 게임 로직 처리                              │
└─────────────────────────────────────────────────────────────────┘
                    ↓
PlayerStateMachine.OnStatChange()
├── playerData.SetStat(StatType.Health, newValue)
├── UIEventDispatcher.DispatchStatChanged() [UI 이벤트 자동 발송]
└── networkHandler.SyncStatsToNetwork() [네트워크 동기화]
                    ↓
┌─────────────────────────────────────────────────────────────────┐
│                    📡 네트워크 동기화                              │
└─────────────────────────────────────────────────────────────────┘
                    ↓
PlayerNetworkHandler.SyncStatsToNetwork()
├── UpdatePlayerProperties()
├── PhotonNetwork.LocalPlayer.SetCustomProperties(props)
└── 다른 클라이언트들에게 스탯 변경 전송
```

---

## 🌍 **환경 시스템 플로우**

```
┌─────────────────────────────────────────────────────────────────┐
│                    🌦️ 환경 시스템 (MasterClient만)               │
└─────────────────────────────────────────────────────────────────┘
                    ↓
NetworkGameEventManager.Update()
├── timeSystem.UpdateTime()
├── seasonSystem.CheckSeasonChange()
├── weatherSystem.UpdateWeather()
└── ApplyEnvironmentalEffects()
                    ↓
┌─────────────────────────────────────────────────────────────────┐
│                    📡 환경 변경 이벤트 전송                        │
└─────────────────────────────────────────────────────────────────┘
                    ↓
PhotonNetwork.RaiseEvent(EnvironmentChange)
├── 날씨 변경 데이터
├── 계절 변경 데이터
└── 시간 변경 데이터
                    ↓
┌─────────────────────────────────────────────────────────────────┐
│                    🎮 모든 클라이언트에서 수신                      │
└─────────────────────────────────────────────────────────────────┘
                    ↓
PlayerStateMachine.OnEnvironmentChange()
├── OnSanityChange() [정신력 변화]
├── OnTemperatureChange() [체온 변화]
├── OnWetnessChange() [젖음 변화]
├── OnHungerChange() [배고픔 변화]
└── OnThirstChange() [수분 변화]
```

---

## 🏗️ **월드 시스템 플로우**

```
┌─────────────────────────────────────────────────────────────────┐
│                    🏗️ 구조물 건설                                │
└─────────────────────────────────────────────────────────────────┘
                    ↓
플레이어가 건설 버튼 클릭
                    ↓
NetworkWorldManager.BuildStructure()
├── BuildStructureRPC() 전송
├── 모든 클라이언트에서 구조물 생성
└── 구조물 데이터 동기화
                    ↓
┌─────────────────────────────────────────────────────────────────┐
│                    🌳 리소스 채집                                │
└─────────────────────────────────────────────────────────────────┘
                    ↓
플레이어가 리소스와 상호작용
                    ↓
NetworkResource.HarvestResource()
├── HarvestResourceRPC() 전송
├── 리소스 양 감소
└── UI 업데이트
```

---

## 📡 **네트워크 동기화 방식**

```
┌─────────────────────────────────────────────────────────────────┐
│                    📡 네트워크 동기화 방식                         │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│ 🔄 실시간 데이터  │    │ 📊 상태 데이터    │    │ 🎯 이벤트 데이터  │
│                 │    │                 │    │                 │
│ • 위치/회전     │    │ • 체력/스탯     │    │ • 공격/채집     │
│ • 애니메이션    │    │ • 인벤토리      │    │ • 환경 변화     │
│ • 이동 상태     │    │ • 장비 상태     │    │ • 게임 이벤트   │
└─────────────────┘    └─────────────────┘    └─────────────────┘
        │                       │                       │
        ▼                       ▼                       ▼
IPunObservable          CustomProperties          RPC/RaiseEvent
OnPhotonSerializeView   SetCustomProperties       PhotonNetwork.RPC
20-60Hz                변경 시에만               필요할 때
```

---

## 🎯 **핵심 컴포넌트 관계도**

```
┌─────────────────────────────────────────────────────────────────┐
│                    🎯 핵심 컴포넌트 관계                          │
└─────────────────────────────────────────────────────────────────┘

                    EventHub (중앙 이벤트 관리)
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   NetworkManager      DataManager        GameManager
        │                  │                  │
        ▼                  ▼                  ▼
  PhotonNetwork      PlayerData        SceneController
        │                  │                  │
        ▼                  ▼                  ▼
  RoomManager      PlayerStateMachine   NetworkWorldManager
        │                  │                  │
        ▼                  ▼                  ▼
   UI Panels       PlayerNetworkHandler  ResourceManager
```

---

## 🔄 **이벤트 시스템 플로우**

```
┌─────────────────────────────────────────────────────────────────┐
│                    🔄 이벤트 시스템 플로우                        │
└─────────────────────────────────────────────────────────────────┘

이벤트 발생
    ↓
EventHub.RaiseEvent(new EventType(data))
    ↓
┌─────────────────────────────────────────────────────────────────┐
│                    📋 구독자들에게 전달                           │
└─────────────────────────────────────────────────────────────────┘
    ↓
구독된 컴포넌트들의 이벤트 핸들러 실행
    ↓
각 컴포넌트별 로직 처리
    ↓
필요시 추가 이벤트 발생 (연쇄 반응)
```

---

## ⚡ **성능 최적화 플로우**

```
┌─────────────────────────────────────────────────────────────────┐
│                    ⚡ 성능 최적화                                 │
└─────────────────────────────────────────────────────────────────┘

PlayerNetworkHandler.Update()
    ↓
ApplyNetworkQualitySettings() [5초마다]
├── 핑 측정
├── SendRate/SerializationRate 조정
└── 보간 설정 최적화
    ↓
ServerManager.Update()
    ↓
UpdateLODSystem()
├── 거리 기반 업데이트 제한
├── nearbyMonsters 계산
└── 불필요한 동기화 방지
```

---

## 🎯 **핵심 요약**

```
🎬 게임 시작: Title → Main → Game 신 전환
🔄 초기화: DefaultExecutionOrder → Awake() 순서 보장
👤 플레이어: 스폰 → 컴포넌트 초기화 → 입력 활성화
📡 동기화: 실시간(60fps) + 상태(이벤트) + 환경(주기적)
🔄 이벤트: 구독 → 발송 → 해제 패턴
⚡ 최적화: 핑 기반 조정 + LOD + 배칭
```

**💡 이 플로우차트로 전체 게임 시스템의 흐름을 한눈에 파악할 수 있습니다!** 🚀✨
